<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <% if current_user %>
  <div class="navbar-header">
    <li><%= image_tag Gravatar.new(current_user.email).image_url, class: "img-circle" %></li>
    <li><h1>Hi <%= current_user.username %>! </h1></li>
  </div>
  <% end %>

  <div class="collapse navbar-collapse navbar-ex1-collapse">

    <% if flash[:notices] %>
    <%= flash[:notices] %>
    <% end %>
    <ul class="nav navbar-nav navbar-right" style="padding-right: 20px">
      <% if current_user %>

      <%= link_to 'My Profile Page', user_path(current_user), class: 'btn btn-default navbar-btn' %>
      <%= link_to 'Create Question', new_question_path, class: 'btn btn-default navbar-btn' %>
      <%= link_to 'Logout', signout_path, method: 'delete', class: 'btn btn-default navbar-btn' %>

      <% unless index_page? %>
      <%= link_to "Questions", root_path, class: 'btn btn-default navbar-btn' %>
      <% end %>

      <% else %>

      <%= form_tag ("/signin")  do %>
      <%=  text_field_tag("username", nil, placeholder: "username") %>
      <%=  password_field_tag("password", nil, placeholder: "password") %>
      <%= submit_tag("signin", class: 'btn btn-default navbar-btn') %>
      <%= link_to "sign up", new_user_path, class: 'btn btn-info navbar-btn' %>
      <% end %>


      <% end %></div>
    </div></nav><br><br>
  </ul>

  <br><br><br>

  <br>

